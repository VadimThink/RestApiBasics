#validation
validation.name=Name isn`t valid, name length should be >= 1 and <= 50
validation.description=Description isn`t valid, description length should be >= 1 and <= 100
validation.price=Price isn`t valid, price should be >= 1
validation.duration=Duration isn`t valid, duration should be >= 1
validation.id=Id should be >= 1
validation.not.available.column=Not available column name, you can use only: "id", "name", \
  "description", "price", "duration", "create_date" and "last_update_date"
validation.not.available.order=Not available order type, you can use only: "DESC", "ASC"
validation.pagination=Invalid pagination
#certificate
certificate.exist=Certificate already exist
certificate.not.created=Certificate wasn`t created
certificate.not.found=Certificate wasn`t found
#tag
tag.exist=Tag already exist
tag.not.found=Tag wasn`t found
tag.list.is.empty=Tag list is empty
#user
user.not.found=User wasn`t found
user.exist=User already exist
#order
order.not.found=Order wasn`t found
#request
request.body.missing=Request body is missing
#exception
exception.duplicate=Such resource is exists already
exception.no.access=No access
#jwt
jwt.expired-invalid=Jwt token is expired or invalid